package SolidDesignPrincipal;

public class DIP {
    public static void main(String[] args) {
        // Dependency Inversion Principle (DIP) states that high-level modules should
        // not
        // depend on low-level modules. Both should depend on abstractions.
        // This principle is about decoupling software modules. It suggests that
        // high-level modules, which provide complex logic, should not depend on
        // low-level modules, which provide utility features. Instead, both should
        // depend
        // on abstractions (e.g., interfaces or abstract classes).
    }
}
